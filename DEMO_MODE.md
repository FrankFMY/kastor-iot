# 🎭 DEMO режим KASTOR IoT

## 📋 Что такое DEMO режим?

DEMO режим автоматически:
- ✅ **Отключает авторизацию** - все страницы доступны без входа
- ✅ **Увеличивает лимиты** - rate limit 500 запросов/мин (вместо 100)
- ✅ **Отключает создание реальных алертов** - используются только данные из seed
- ✅ **Включает demo-пользователя** - автоматически создается admin пользователь

## 🔍 Как проверить статус DEMO режима?

### Способ 1: Через API (рекомендуется)

```bash
curl http://localhost:3000/api/health | jq .demoMode
```

Или откройте в браузере: `http://your-domain/api/health`

В ответе будет поле `demoMode: true/false`

### Способ 2: Через скрипт

```bash
./scripts/check-demo-mode.sh
```

### Способ 3: Проверка переменных окружения

```bash
docker exec kastor-app printenv | grep DEMO_MODE
docker exec kastor-app printenv | grep NODE_ENV
```

## ⚙️ Как включить/выключить DEMO режим?

### Вариант 1: Через переменную окружения (рекомендуется)

**Включить:**
```bash
export DEMO_MODE=true
docker compose -f docker-compose.production.yaml --profile demo up -d
```

**Выключить:**
```bash
export DEMO_MODE=false
docker compose -f docker-compose.production.yaml --profile demo up -d
```

### Вариант 2: Через .env файл

Создайте файл `.env` в корне проекта:

```bash
# Включить DEMO режим
DEMO_MODE=true

# Или выключить
DEMO_MODE=false
```

Затем перезапустите:
```bash
docker compose -f docker-compose.production.yaml --profile demo up -d
```

### Вариант 3: Прямо в docker-compose.production.yaml

Отредактируйте строку 82:
```yaml
DEMO_MODE: ${DEMO_MODE:-true}  # true = включен, false = выключен
```

## 📊 Текущий статус

**По умолчанию DEMO режим ВКЛЮЧЕН** (`DEMO_MODE:-true` в docker-compose)

DEMO режим активен если:
- `DEMO_MODE=true` **ИЛИ**
- `NODE_ENV=development`

## 🎯 Что происходит в DEMO режиме?

### ✅ Включено:
- Все страницы доступны без авторизации
- Rate limit: 500 запросов/минуту
- Автоматический demo-пользователь с правами admin
- Отключено создание реальных алертов из телеметрии
- Используются только данные из seed скрипта

### ❌ Отключено:
- Проверка авторизации на страницах
- Redirect на `/login`
- Создание реальных алертов из MQTT телеметрии

## 🔧 Быстрая проверка после запуска

```bash
# 1. Проверить статус через API
curl http://localhost:3000/api/health | jq '{demoMode, status}'

# 2. Проверить переменные в контейнере
docker exec kastor-app printenv | grep -E "DEMO_MODE|NODE_ENV"

# 3. Попробовать открыть страницу без авторизации
curl -I http://localhost:3000/comparison
# Должен вернуть 200, а не 302 (redirect на login)
```

## 📝 Примеры использования

### Для презентации инвесторам:
```bash
# Убедитесь что DEMO режим включен
export DEMO_MODE=true
docker compose -f docker-compose.production.yaml --profile demo up -d

# Проверьте статус
curl http://localhost:3000/api/health | jq .demoMode
# Должно быть: true
```

### Для production (после презентации):
```bash
# Выключите DEMO режим
export DEMO_MODE=false
docker compose -f docker-compose.production.yaml --profile demo up -d

# Проверьте статус
curl http://localhost:3000/api/health | jq .demoMode
# Должно быть: false
```

## ⚠️ Важно!

- **Для демонстрации**: DEMO режим должен быть **ВКЛЮЧЕН** (`true`)
- **Для production**: DEMO режим должен быть **ВЫКЛЮЧЕН** (`false`)
- По умолчанию в `docker-compose.production.yaml` DEMO режим **ВКЛЮЧЕН**
